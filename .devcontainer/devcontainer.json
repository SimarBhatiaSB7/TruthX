{
    "name": "TruthX-GPU-Env",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "runArgs": [
        "--gpus", "all",
        "--shm-size", "8gb"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-toolsai.jupyter"
            ]
        }
    },
    "postCreateCommand": "cd /workspaces/TruthX/transformers && pip install -e . && cd /workspaces/TruthX && [ ! -d FastChat ] && git clone https://github.com/lm-sys/FastChat.git || true && cd FastChat && pip install -e ."
}